# tutorial/urls.py
from django.contrib import admin
# from .views import SequenceView
# from .views import PoseView
from django.urls import path



urlpatterns = [
    # path('sequence/', SequenceView.as_view(), name='sequence'),
    # path('sequence/<int:pk>/', SequenceView.as_view()),
    # path('pose/', PoseView.as_view(), name='pose'),
    # path('pose/<int:pk>/', PoseView.as_view()),
]
